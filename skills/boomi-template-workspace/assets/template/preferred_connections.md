# Connection Registry
# List your Boomi connections here so the agent can find and re-use them.
# Format is free-form — just describe the connection and paste the platform link.
# The agent reads this file and matches entries by context to the connectors it needs.
#
# Examples:
#
# Salesforce - production org
# https://platform.boomi.com/AtomSphere.html#build;accountId=YOUR_ACCOUNT;components=YOUR_COMPONENT_ID
#
# PostgreSQL - customer orders database
# https://platform.boomi.com/AtomSphere.html#build;accountId=YOUR_ACCOUNT;components=YOUR_COMPONENT_ID
